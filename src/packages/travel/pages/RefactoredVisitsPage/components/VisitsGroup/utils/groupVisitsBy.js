import { GROUP_VISITS_BY } from '../../../consts';

export default function groupVisitsBy(visitsList, groupBy) {
  const key = resolveKey;
  visitsList.reduce((accumulator, visit) => {}, {});

  return [[groupBy, visitsList]];
}
