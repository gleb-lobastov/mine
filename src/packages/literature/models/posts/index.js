import * as adapters from './adapters';
import * as consts from './consts';
import model from './model';

export default { adapters, consts, model };
export const articlesAdapters = adapters;
export const articlesConsts = consts;
export const articlesModel = model;
